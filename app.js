document.addEventListener("DOMContentLoaded", () => {
    let data = [];
    let filtros = {
        periodo: "",
        clase: "Todas",
        sucursal: "",
        oficial: ""
    };
    // Código de carga y render similar al entregado antes...
});